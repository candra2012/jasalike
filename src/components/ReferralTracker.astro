<script is:inline>
  const params = new URLSearchParams(window.location.search);
  const ref = params.get('ref');

  if (ref) {
    localStorage.setItem('affiliate_ref', ref);
    console.log('Referral tersimpan:', ref);
  }
</script>
