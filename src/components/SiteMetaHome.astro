---
/**
 * SiteMetaHome.astro — Meta tag lengkap untuk halaman Home
 * Dipakai di slot <head> pada `src/pages/index.astro`.
 */

interface Props {
  title?: string;
  description?: string;
  keywords?: string[] | string;
  url?: string;                 // absolute URL halaman (canonical)
  siteName?: string;            // nama situs/brand
  image?: string;               // OG/Twitter image (boleh relative)
  imageAlt?: string;
  imageWidth?: number;
  imageHeight?: number;
  noindex?: boolean;
  locale?: string;
  alternateLocales?: string[];
  themeColor?: string;
}

const {
  title = 'Jasa Like Terpercaya Murah & Aman',
  description = 'Jasa like murah Terpercaya proses cepat dan aman. Tingkatkan engagement postingan Anda sekarang juga',
  keywords = ['jasa like', 'jasa like aktif', 'jasalike', 'jasa like cepat', 'jasa like terpercaya'],
  url = 'https://jasalike.com/',
  siteName = 'Jasa Like',
  image = '/og-home.png',
  imageAlt = 'Jasa Like — Layanan Like Sosial Media',
  imageWidth = 1200,
  imageHeight = 630,
  noindex = false,
  locale = 'id_ID',
  alternateLocales = [],
  themeColor = '#ffffff',
}: Props = Astro.props

// Helpers
const siteOrigin = Astro.site?.href?.replace(/\/$/, '') || '';
const ensureAbs = (src: string) =>
  src.startsWith('http://') || src.startsWith('https://')
    ? src
    : `${siteOrigin}${src.startsWith('/') ? '' : '/'}${src}`;

const canonical = url.replace(/\/?$/, '/'); // pastikan trailing slash
const ogImage = ensureAbs(image);
const kw = Array.isArray(keywords) ? keywords.join(', ') : keywords;
---

<!-- Primary -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
{kw && <meta name="keywords" content={kw} />}
<meta name="robots" content={noindex ? 'noindex, nofollow' : 'index, follow'} />
<link rel="canonical" href={canonical} />

<!-- Open Graph -->
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:url" content={canonical} />
<meta property="og:site_name" content={siteName} />
<meta property="og:locale" content={locale} />
{alternateLocales.map((l) => (
  <meta property="og:locale:alternate" content={l} />
))}
<meta property="og:image" content={ogImage} />
<meta property="og:image:secure_url" content={ogImage} />
<meta property="og:image:alt" content={imageAlt} />
<meta property="og:image:width" content={String(imageWidth)} />
<meta property="og:image:height" content={String(imageHeight)} />

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />

<!-- PWA / Theming -->
<meta name="theme-color" content={themeColor} />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
